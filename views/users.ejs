<%- include('header', {title: title}) %>

<table class="ui compact celled definition inverted table">
  <thead class="full-width">
    <tr>
      <th>Tools</th>
      <th>Username</th>
      <th>API Key</th>
      <th>Created Timestamp</th>
      <th>Flag</th>
      <th>Status</th>
    </tr>
  </thead>
  <tbody>
    <% for(var i=0; i < users.length; i++) { %>
     <tr>
       <td class="collapsing">
         <div class="ui fitted slider checkbox">
           <button class="ui icon black button">
            <i class="edit icon"></i>
          </button>
         </div>
       </td>
       <td><%= users[i].name %></td>
       <td><%= users[i].key %></td>
       <td><%= users[i].timestamp.toISOString() %></td>
       <td><%= users[i].flag %></td>
       <td><%= users[i].message %></td>
     </tr>
    <% } %>
  </tbody>
  <tfoot class="full-width">
    <tr>
      <th></th>
      <th colspan="5">
        <div class="ui right floated small labeled icon black button">
          <i class="user icon"></i> Add User
        </div>
      </th>
    </tr>
  </tfoot>
</table>

<%- include('footer') %>
